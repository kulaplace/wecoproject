<%= form_for(@note) do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<!--   <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :lead_id %><br>
    <%= f.number_field :lead_id %>
  </div> -->
  <div class="field">
    <%= f.label :notes_body %><br>
    <%= f.text_area :notes_body %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <% @status_array.each do |choose| %>
      <%= f.radio_button(:status, @status_array.index(choose)) %><%= choose %>
      <br />
    <% end %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
